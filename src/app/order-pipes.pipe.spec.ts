import { OrderPipesPipe } from './order-pipes.pipe';

describe('OrderPipesPipe', () => {
  it('create an instance', () => {
    const pipe = new OrderPipesPipe();
    expect(pipe).toBeTruthy();
  });
});
